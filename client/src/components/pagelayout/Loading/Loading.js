import React from "react";
import load from "../../../images/load.gif";

const Loading = () => {
    return ( <
        div >
        <
        img src = { load }
        alt = "loading" / >
        <
        /div>
    );
};

export default Loading;